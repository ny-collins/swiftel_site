# Node.js dependencies
# This folder is huge and can be rebuilt by running `npm install`
/node_modules

# Astro build cache and output
# These are generated files and shouldn't be tracked
/.astro
/dist

# Environment variables
# These files contain sensitive secrets (API keys, passwords)
.env
.env.*
!.env.example

# VS Code editor configuration
# This ignores user-specific settings but allows sharing team settings
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json

# Operating System files
# General files generated by macOS, Windows, and Linux
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
